Read `/app/index.txt` and create `/app/report.json`.

1. Count the number of non-empty lines in `/app/index.txt`.
2. Write the result to `/app/report.json` as valid JSON using the key `non_empty_lines`.
3. The value of `non_empty_lines` must be an integer representing the count.
